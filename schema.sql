CREATE TABLE sad_words (
    messageID VARCHAR(100) NOT NULL,
    userID INT NOT NULL,
    msg TEXT NOT NULL
);

CREATE TABLE encouragements (
    messageID VARCHAR(100) NOT NULL,
    userID INT NOT NULL,
    msg TEXT NOT NULL
);